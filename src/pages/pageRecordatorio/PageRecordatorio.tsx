import AppRecordatorio from "@/components/Recordatorio/AppRecordatorio";
import { FC } from "react";

const PageRecordatorio: FC = () => {
  return (
    <>
      <AppRecordatorio />
    </>
  );
};

export default PageRecordatorio;
